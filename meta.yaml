# cmd: conda build . -c auto-mech -c psi4 -c rdkit -c openbabel -c conda-forge
package:
    name: elstruct
    version: "0.3.0"

source:
    path: .

requirements:
    build:
        - python
    run:
        - python
        - numpy
        - scipy
        - pyyaml
        - mako
        - pytest
        - pytest-cov
        - future
        - flake8
        - pylint
        - rdkit::rdkit
        - openbabel::openbabel
        - psi4::psi4
        - auto-mech::autoparse
        - auto-mech::automol
        - conda-forge::qcelemental
